VITE_API_URL=http://localhost:5000/api
# Same value as server ADMIN_API_KEY — sent as Authorization: Bearer for admin UI (bookings list, physios, release, disputes, verifications)
VITE_ADMIN_API_KEY=change_admin_key
