# Read at `vite build` (production) for dist/sitemap.xml and dist/robots.txt.
# CI or Render can override via VITE_PUBLIC_SITE_URL in the build environment.
VITE_PUBLIC_SITE_URL=https://physiokhom.com
